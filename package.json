{
  "name": "@quantecon/plugins.mystmd",
  "version": "0.0.0",
  "private": true,
  "description": "QuantEcon plugin families for the mystmd engine, shipped as single-file .mjs bundles.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "test:unit": "node --test tests/unit",
    "test:plugin": "npm run bundle && node --test tests/plugin",
    "test": "npm run test:unit && npm run test:plugin"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
